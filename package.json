{
  "name": "sectec-monorepo",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:all": "npm run build --prefix frontend && cd backend && npm run build",
    "railway-build": "npm run install:all && npm run build:all",
    "start": "node backend/dist/src/main.js" 
  }
}
  